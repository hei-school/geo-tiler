openapi: 3.0.3
info:
  title: geo-tiler
  description: geo-tiler
  version: 1.0.0
servers:
  - url: 'https://localhost:8080'
paths:
  '/ping':
    get:
      tags:
        - Health
      responses:
        200:
          description: pong
          content:
            text/plain:
              schema:
                type: string
                example: pong